[workspace]
resolver = "3"
members = [
    "crates/riley-comments-core",
    "crates/riley-comments-api",
    "crates/riley-comments-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.94.1"

[workspace.dependencies]
# async runtime
tokio = { version = "1", features = ["full"] }

# web
axum = { version = "0.8", features = ["multipart"] }
tower-http = { version = "0.6", features = ["cors", "trace"] }
tower = "0.5"

# database
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "postgres", "chrono", "uuid"] }

# serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

# auth
jsonwebtoken = "9"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }

# types
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4", "v7", "serde"] }

# error handling
thiserror = "2"
anyhow = "1"

# observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# cli
clap = { version = "4", features = ["derive", "env"] }

# object storage (R2)
# default-features = false drops the "rustls" feature, which pulls in the legacy
# hyper 0.14 / rustls 0.21 client (h2 0.3, rustls-webpki 0.101 RustSec advisories).
aws-sdk-s3 = { version = "1", default-features = false, features = [
    "behavior-version-latest",
    "default-https-client",
    "rt-tokio",
    "sigv4a",
    "http-1x",
] }
aws-config = { version = "1", features = ["behavior-version-latest"] }
aws-credential-types = "1"
